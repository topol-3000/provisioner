---
phase: 03-registry-create-path-fake-adapter
plan: 04
type: execute
wave: 3
depends_on:
  - 03-03
files_modified:
  - tests/provisioning/test_models.py
  - tests/provisioning/test_spec.py
  - tests/provisioning/test_handlers.py
  - tests/provisioning/test_service.py
  - tests/provisioning/test_tasks.py
  - tests/conftest.py
autonomous: true
requirements:
  - PROV-01
  - PROV-02
  - PROV-03
  - PROV-04
  - PROV-08
  - SNAP-01

must_haves:
  truths:
    - "make test (fast, Docker-free) is fully green — all unit tests pass without containers"
    - "make test-integration is fully green — all testcontainers-backed integration tests pass"
    - "PROV-01 UNIQUE constraint on subscription_id is verified by an integration test that attempts a duplicate insert"
    - "PROV-04 canonical proof: FakeDeploymentAdapter(fail_on={'create'}, fail_count=1) + FakeClock → failed → retry → ready in a single test"
    - "PROV-08 credential delivery: ConsoleNotificationTransport.send_credentials called exactly once; a retry reaching ready again does NOT call it again"
    - "SNAP-01 enforcement_snapshot: row exists with version=1, module_set/seat_cap from spec, instance.snapshot_version=1 after a completed create task"
    - "test_tasks.py and test_service.py stubs from Wave 0 are replaced with full real-assertion test suites"
  artifacts:
    - path: "tests/provisioning/test_tasks.py"
      provides: "Full task test coverage: happy path, fault injection retry, credential-once, snapshot write (replaces Wave 0 stub)"
      contains: "test_create_fails_then_retries"
    - path: "tests/provisioning/test_service.py"
      provides: "Full service state-machine unit tests: open_instance, validate_transition, write_enforcement_snapshot delegation (replaces Wave 0 stub)"
      contains: "test_validate_transition_rejects_invalid"
    - path: "tests/provisioning/test_models.py"
      provides: "Column/constraint assertions + integration uuid7 + UNIQUE violation test"
      contains: "test_instance_columns"
    - path: "tests/provisioning/test_spec.py"
      provides: "InstanceSpec builder unit tests — D-03 no line_count mapping, slug derivation, frozen, round-trip"
      contains: "test_spec_uses_settings_defaults"
    - path: "tests/provisioning/test_handlers.py"
      provides: "Real-body handler assertions: opens instance+task rows, binds instance_id, registers post-commit enqueue"
      contains: "test_activated_opens_instance_row"
  key_links:
    - from: "tests/provisioning/test_tasks.py::test_create_path_succeeds"
      to: "instance.status == ready"
      via: "InMemoryBroker(await_inplace=True) + FakeDeploymentAdapter + pg_session"
      pattern: "test_create_path_succeeds"
    - from: "tests/provisioning/test_tasks.py::test_create_fails_then_retries"
      to: "PROV-04 ROADMAP criterion 3"
      via: "FakeDeploymentAdapter(fail_on={'create'}, fail_count=1) + FakeClock"
      pattern: "test_create_fails_then_retries"
    - from: "tests/provisioning/test_service.py::test_write_enforcement_snapshot_calls_repository"
      to: "WARNING 4 fix proof"
      via: "mock repository — assert insert_enforcement_snapshot + update_snapshot_version called, never raw ORM"
      pattern: "test_write_enforcement_snapshot"
    - from: "tests/provisioning/test_spec.py::test_instance_spec_round_trip"
      to: "WARNING 5 fix proof"
      via: "spec.to_dict() → InstanceSpec.from_dict() → assert fields equal; tuple preserved"
      pattern: "test_instance_spec_round_trip"
---

<objective>
Complete the Nyquist sampling requirement: every test identified in VALIDATION.md Wave 0 is implemented, runnable, and green. This plan replaces the Wave 0 stubs in test_tasks.py and test_service.py with full real-assertion suites, and fills any remaining gaps in test_models.py, test_spec.py, test_handlers.py, and conftest.py.

Purpose: Phase cannot be verified without a green test suite. This plan closes all open rows in the VALIDATION.md per-requirement test map. Plans 01-03 laid out the behavior and wrote the Wave 0 stubs; this plan locks it down.
Output: Full test suite — test_models.py, test_spec.py, test_handlers.py, test_service.py (stub → full), test_tasks.py (stub → full) complete and green; conftest.py broker fixture wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-registry-create-path-fake-adapter/03-CONTEXT.md
@.planning/phases/03-registry-create-path-fake-adapter/03-VALIDATION.md
@.planning/phases/03-registry-create-path-fake-adapter/03-PATTERNS.md
@.planning/phases/03-registry-create-path-fake-adapter/03-01-SUMMARY.md
@.planning/phases/03-registry-create-path-fake-adapter/03-02-SUMMARY.md
@.planning/phases/03-registry-create-path-fake-adapter/03-03-SUMMARY.md
</context>

<interfaces>
<!-- Tests target the code delivered in Plans 01-03. Read these before writing tests. -->

From tests/conftest.py (current state — session-scoped pg_engine, function-scoped pg_session, plus ENUM types added in Plan 01):
```python
@pytest.fixture(scope="session")
async def pg_engine(postgres_container) -> AsyncIterator[AsyncEngine]:
    # Creates schema, ENUM types (added in Plan 01), then Base.metadata.create_all

@pytest.fixture
async def pg_session(pg_engine) -> AsyncIterator[AsyncSession]:
    # Function-scoped; truncates all tables after each test
```

From tests/provisioning/test_idempotency.py (existing integration test pattern):
```python
def _patch_session_scope(monkeypatch, session):
    @asynccontextmanager
    async def _scope():
        yield session
    monkeypatch.setattr(ec, "session_scope", _scope)

@pytest.mark.integration
async def test_first_delivery_writes_one_row(pg_session, monkeypatch) -> None:
    _patch_session_scope(monkeypatch, pg_session)
    ...
```

From tests/provisioning/test_handlers.py (existing no-op tests — to update):
```python
# Current: parametrize all 5 handlers as no-ops
# Phase 3: remove handle_subscription_activated from no-op list; add real-body tests

def _mock_env() -> MagicMock:
    return MagicMock(id="01JZQABCDE12345678901234AB", correlation_id=None)
```

From ports/deployment_adapter.py (InstanceSpec and round-trip — WARNING 5 fix):
```python
@dataclass(frozen=True, slots=True)
class InstanceSpec:
    # ... fields ...
    def to_dict(self) -> dict: ...        # tuple→list, Mapping→dict, UUID→str
    @classmethod
    def from_dict(cls, data: dict) -> "InstanceSpec": ...  # list→tuple, reconstructs all fields
```

From modules/provisioning/service.py (WARNING 4 fix — write_enforcement_snapshot):
```python
class ProvisioningService:
    async def write_enforcement_snapshot(
        self, session: AsyncSession, instance_id: UUID, spec: InstanceSpec, version: int = 1,
    ) -> None:
        # calls repository.insert_enforcement_snapshot + repository.update_snapshot_version
        # test_service.py must assert these two repository calls are made
```

From RESEARCH.md §Testing (InMemoryBroker pattern):
```python
from taskiq import InMemoryBroker
broker = InMemoryBroker(await_inplace=True, propagate_exceptions=True)
broker.add_dependency_context({
    DeploymentAdapter: fake_adapter,
    NotificationTransport: console_transport,
    Clock: fake_clock,
    Settings: test_settings,
    ProvisioningService: service_instance,
})
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: test_models.py + test_spec.py + test_handlers.py completions + conftest broker fixture</name>
  <files>
    tests/provisioning/test_models.py,
    tests/provisioning/test_spec.py,
    tests/provisioning/test_handlers.py,
    tests/conftest.py
  </files>
  <read_first>
    - tests/provisioning/test_models.py (current state — existing ProcessedEvent tests; extend, do not replace)
    - tests/provisioning/test_handlers.py (current state — existing no-op parametrize tests; update per PATTERNS.md §test_handlers.py)
    - tests/conftest.py (current state — verify pg_engine already has ENUM creation from Plan 01; add in_memory_broker fixture)
    - .planning/phases/03-registry-create-path-fake-adapter/03-PATTERNS.md §test_models.py §test_handlers.py §conftest.py (exact new assertion patterns)
    - src/provisioning_worker/modules/provisioning/models.py (Instance, ProvisioningTask, EnforcementSnapshot — column names, constraints, enum values)
    - src/provisioning_worker/ports/deployment_adapter.py (InstanceSpec with to_dict/from_dict — defined HERE not in spec.py)
    - src/provisioning_worker/modules/provisioning/spec.py (build_instance_spec — imports InstanceSpec from ports)
    - src/provisioning_worker/modules/provisioning/handlers.py (real body — what to assert against)
    - src/provisioning_worker/settings.py (provisioning_default_seat_cap for spec tests)
  </read_first>
  <behavior>
    test_models.py new assertions:
    - test_instance_tablename: Instance.__tablename__ == "instance"
    - test_instance_schema: Instance.__table__.schema == "provisioning"
    - test_instance_columns: {c.name for c in Instance.__table__.columns} == expected full set
    - test_instance_subscription_id_unique: any(c.unique for c in Instance.__table__.columns if c.name == "subscription_id") is True
    - test_provisioning_task_fk: FK from provisioning_task.instance_id to provisioning.instance.id exists
    - test_enforcement_snapshot_pk: {c.name for c in EnforcementSnapshot.__table__.primary_key} == {"instance_id"}
    - test_base_metadata_includes_new_tables: "provisioning.instance" in Base.metadata.tables
    - test_instance_status_values: set(InstanceStatus.__members__) has pending, deploying, configuring, ready, failed, suspended, deprovisioning, deprovisioned
    - test_uuid_pk_version (integration, @pytest.mark.integration): insert Instance row, assert .id.version == 7
    - test_subscription_id_unique_violation (integration): insert two Instances with same subscription_id → IntegrityError (PROV-01 UNIQUE constraint proof)

    test_spec.py new assertions (these may already be implemented from Plan 01 — verify and fill gaps):
    - test_spec_uses_settings_defaults: build_instance_spec with line_count=5, seat_cap_default=10 → spec.seat_cap == 10 (not 5)
    - test_spec_slug_derivation: subscription_id "018efa2c-0000-7000-8000-000000000001" → spec.slug == "018efa2c.example.local" (or matches the settings.instance_domain_suffix pattern)
    - test_spec_module_set_is_tuple: isinstance(spec.module_set, tuple)
    - test_spec_is_frozen: attempting spec.seat_cap = 99 raises an error (frozen dataclass)
    - test_spec_resource_caps_is_mapping: spec.resource_caps is an instance of Mapping
    - test_instance_spec_round_trip (WARNING 5 proof): spec.to_dict() → InstanceSpec.from_dict() → assert reconstructed spec equals original; assert type(reconstructed.module_set) is tuple (not list); assert isinstance(reconstructed.resource_caps, dict)

    test_handlers.py updates:
    - Remove handle_subscription_activated from the _ALL_HANDLERS no-op parametrize list
    - Keep test_handler_is_no_op for the other 4 handlers
    - Add test_activated_opens_instance_row: call handler with a mock session; assert session.add was called with an Instance object (mock AsyncSession, assert add called with isinstance arg)
    - Add test_activated_opens_task_row: session.add called with a ProvisioningTask object
    - Add test_activated_binds_instance_id_after_insert: monkeypatch bind_contextvars; assert "instance_id" key is in the bound context
    - Add test_activated_registers_post_commit_not_inline: mock broker kiq; assert kiq was NOT called inline during handler execution (only after commit, via post-commit hook)

    tests/conftest.py: Verify (and add if missing) the in_memory_broker fixture wired with InMemoryBroker(await_inplace=True). Add ProvisioningService to the dependency context (since Task 1 of Plan 03-03 added it to the task signature). Add a `spy_console_transport` fixture that wraps ConsoleNotificationTransport with a call counter (AsyncMock or a simple spy class) for test_credentials_sent_once assertion.
  </behavior>
  <action>
    tests/provisioning/test_models.py: Extend (do not replace) — add the new Phase 3 table assertions after the existing ProcessedEvent tests. Use the same unit test pattern (no async, no DB, just ORM metadata inspection). Integration tests marked with `@pytest.mark.integration` use `pg_session` fixture and insert real rows.

    tests/provisioning/test_spec.py: All pure synchronous unit tests. Build SubscriptionActivatedPayload with minimal required fields (subscription_id, customer_id, line_count=5, total_amount=Decimal("50.00"), activated_at=datetime.now()). Build Settings with overridden `provisioning_default_seat_cap=10`. Call `build_instance_spec(payload, settings, entitlement)` where entitlement is `EntitlementPicture(module_set=(), seat_cap=10, resource_caps={})`. Assert spec.seat_cap == 10 and spec.seat_cap != 5. For the round-trip test: build a spec, call `spec.to_dict()`, then `InstanceSpec.from_dict(spec.to_dict())`. Import `InstanceSpec` from `provisioning_worker.ports.deployment_adapter` (not from spec.py — it is defined in ports).

    tests/provisioning/test_handlers.py: Update per behavior above. For real-body tests, use `AsyncMock()` as the session and `MagicMock()` for raw_env/payload. The handler now calls `session.add(instance)` and `session.add(task)` — use `call_args_list` to check both adds. For the post-commit hook test, monkeypatch `register_post_commit` from `shared.event_consumer` to capture the registered callback; assert the broker's kiq is not called during handler execution.

    tests/conftest.py: Add `in_memory_broker` fixture (function-scoped) that creates `InMemoryBroker(await_inplace=True, propagate_exceptions=True)` and wires `add_dependency_context` with `FakeDeploymentAdapter()`, `FakeClock()`, a spy `ConsoleNotificationTransport`, `Settings()` with test defaults, and `ProvisioningService(entitlement_resolver=DefaultEntitlementResolver())`. The broker must have tasks registered — import `modules.provisioning.tasks` before returning the broker. Add `spy_console_transport` fixture that is an AsyncMock-wrapped ConsoleNotificationTransport recording `send_credentials` calls.
  </action>
  <verify>
    <automated>
      .venv/bin/pytest tests/provisioning/test_models.py tests/provisioning/test_spec.py tests/provisioning/test_handlers.py -x -m "not integration" 2>&amp;1 | tail -20
    </automated>
  </verify>
  <done>
    test_models.py: all column/constraint/metadata assertions pass (unit). test_spec.py: all builder assertions pass including round-trip (WARNING 5 proof). test_handlers.py: no-op parametrize covers 4 handlers; real-body assertions for activated handler pass. conftest.py in_memory_broker fixture wired with ProvisioningService. make check passes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test_tasks.py + test_service.py — replace stubs with full suites + integration green</name>
  <files>
    tests/provisioning/test_tasks.py,
    tests/provisioning/test_service.py
  </files>
  <read_first>
    - tests/provisioning/test_tasks.py (current state — Wave 0 stub with pytest.skip placeholder; REPLACE entirely with real tests)
    - tests/provisioning/test_service.py (current state — Wave 0 stub with pytest.skip placeholder; REPLACE entirely with real tests)
    - tests/provisioning/test_idempotency.py (full file — integration test pattern with _patch_session_scope, pg_session, @pytest.mark.integration)
    - tests/conftest.py (pg_session, in_memory_broker, spy_console_transport fixtures from Task 1)
    - src/provisioning_worker/modules/provisioning/tasks.py (create_instance_task — the code under test)
    - src/provisioning_worker/modules/provisioning/service.py (ProvisioningService — the code under test, including write_enforcement_snapshot)
    - src/provisioning_worker/adapters/fake_deployment.py (FakeDeploymentAdapter fault injection API)
    - src/provisioning_worker/ports/clock.py (FakeClock — no-op sleep; deterministic now())
    - src/provisioning_worker/ports/deployment_adapter.py (InstanceSpec — imported from here)
    - .planning/phases/03-registry-create-path-fake-adapter/03-VALIDATION.md (per-requirement test map — every row must have a matching test)
    - .planning/phases/03-registry-create-path-fake-adapter/03-RESEARCH.md §Testing (InMemoryBroker pattern, PROV-04 canonical proof test structure)
    - .planning/phases/03-registry-create-path-fake-adapter/03-PATTERNS.md §test_tasks.py §test_service.py
  </read_first>
  <behavior>
    test_tasks.py — unit tests (no DB, in-memory):
    - test_backoff_formula: _compute_backoff_seconds(0, settings) == 2.0; _compute_backoff_seconds(1, settings) == 4.0; _compute_backoff_seconds(10, settings_cap60) == 60.0
    - test_create_path_succeeds (unit with mock session): FakeDeploymentAdapter() + FakeClock(); run create_instance_task via InMemoryBroker(await_inplace=True); assert instance.status transitions to ready, task.status to succeeded
    - test_credentials_sent_once (unit): spy_console_transport; successful run → send_credentials called exactly once
    - test_no_credential_resend_on_retry (unit): spy_console_transport; FakeDeploymentAdapter(fail_on={"create"}, fail_count=1) + FakeClock; run task (fails first, retries, succeeds) → send_credentials called exactly once total (NOT twice)
    - test_create_fails_then_retries (unit with FakeClock no-op sleep — PROV-04 canonical proof): FakeDeploymentAdapter(fail_on={"create"}, fail_count=1); after first task execution, instance.status == "failed", task.attempt_count == 1, task.last_error is not None; after second execution (auto-retry), instance.status == "ready"
    - test_consumer_does_not_crash_on_adapter_failure: task internal exception does not propagate out of create_instance_task (the exception is handled and the task returns None)
    - test_spec_rebuilt_from_payload (unit): task.payload is populated with spec.to_dict() by the handler; task rebuilds InstanceSpec via InstanceSpec.from_dict(task.payload); assert rebuilt spec.module_set is a tuple (WARNING 5 round-trip proof in the task context)

    test_tasks.py — integration tests (@pytest.mark.integration with pg_session):
    - test_create_path_succeeds_integration: uses pg_session + real DB rows; instance.status == "ready" after task; instance.url is not None
    - test_enforcement_snapshot_written: after task, EnforcementSnapshot row in DB with version=1; instance.snapshot_version == 1 (SNAP-01 proof)
    - test_subscription_id_unique_on_second_activated: two subscription.activated with same subscription_id → second one is a no-op (dedupe via processed_event); only one instance row

    test_service.py — unit tests:
    - test_open_instance_returns_instance_and_task: mock session; call service.open_instance(payload, session, settings); returns (Instance, ProvisioningTask) with correct types
    - test_open_instance_stages_both_rows: mock session; assert session.add called twice (once for Instance, once for ProvisioningTask)
    - test_open_instance_sets_pending_status: returned instance.status == InstanceStatus.pending
    - test_open_instance_payload_contains_spec_dict: returned task.payload is a dict (from spec.to_dict()) with key "subscription_id"; confirms WARNING 5 contract is wired in open_instance
    - test_validate_transition_allows_valid: validate_transition(pending, deploying) does not raise; validate_transition(deploying, configuring) does not raise; validate_transition(configuring, ready) does not raise
    - test_validate_transition_rejects_invalid: validate_transition(ready, pending) raises InvalidTransition; validate_transition(pending, ready) raises InvalidTransition (skipping steps)
    - test_validate_transition_any_to_failed: validate_transition(deploying, failed) does not raise (any step can fail)
    - test_write_enforcement_snapshot_calls_repository (WARNING 4 proof): mock session, mock repository functions; call service.write_enforcement_snapshot(session, instance_id, spec); assert mock_repository.insert_enforcement_snapshot was called with (session, instance_id, spec, 1) and mock_repository.update_snapshot_version was called with (session, instance_id, 1); assert NO direct ORM session.add for EnforcementSnapshot was made by the service itself (repository owns that)
  </behavior>
  <action>
    tests/provisioning/test_service.py: REPLACE the Wave 0 stub entirely. Pure unit tests (no async DB required for state machine tests). Use `AsyncMock()` for session. Test ProvisioningService.open_instance, validate_transition, and write_enforcement_snapshot. Follow the mock session pattern from test_idempotency.py. For `test_write_enforcement_snapshot_calls_repository`: monkeypatch `repository.insert_enforcement_snapshot` and `repository.update_snapshot_version` with AsyncMock; call `await service.write_enforcement_snapshot(mock_session, instance_id, spec, version=1)`; assert each was called once with the correct args.

    tests/provisioning/test_tasks.py: REPLACE the Wave 0 stub entirely. Unit tests use InMemoryBroker(await_inplace=True) from conftest.py `in_memory_broker` fixture or constructed inline for fault-injection parameterization. FakeDeploymentAdapter constructed inline (not as fixture) for tests that need fault injection to keep parameterization local. FakeClock ensures `asyncio.sleep` in backoff is a no-op — tests run instantly. Integration tests use `pg_session` + `@pytest.mark.integration`.

    For the PROV-04 canonical proof (`test_create_fails_then_retries`): construct `FakeDeploymentAdapter(fail_on={"create"}, fail_count=1)` and `FakeClock()`. Run `create_instance_task` via the InMemoryBroker or by calling the task directly. After the first invocation: assert `instance.status == InstanceStatus.failed` (by querying the DB row or inspecting the mock session). The re-kick (via asyncio sleep no-op + kiq()) should auto-trigger in the await_inplace broker. After the second invocation: assert `instance.status == InstanceStatus.ready`.

    For credential-delivery tests: use the `spy_console_transport` fixture (AsyncMock wrapping `send_credentials`). Assert `spy_console_transport.send_credentials.call_count == 1` after both unit and integration runs.

    For the snapshot integration test: use `pg_session` with `monkeypatch` to inject session_scope. After `create_instance_task` completes: query `EnforcementSnapshot` by instance_id. Assert `snapshot.version == 1`, `snapshot.seat_cap == settings.provisioning_default_seat_cap`, `snapshot.module_set == []` (M1 default). Query `Instance` and assert `instance.snapshot_version == 1`.

    For `test_spec_rebuilt_from_payload`: construct a task row where `task.payload = spec.to_dict()`. Assert that `InstanceSpec.from_dict(task.payload).module_set` is a tuple (not list) and equals the original `spec.module_set`. This confirms the round-trip contract holds in the task's actual execution path.

    Final gate: after Task 2, run `make test` and `make test-integration` (which requires Docker). Both must be green. `make check` must pass for the entire test suite.
  </action>
  <verify>
    <automated>
      .venv/bin/pytest tests/provisioning/test_tasks.py tests/provisioning/test_service.py -x -m "not integration" 2>&amp;1 | tail -20
    </automated>
    <human-check>
      Run `make test-integration` (requires Docker + testcontainers). Confirm all integration tests pass including test_create_path_succeeds_integration, test_enforcement_snapshot_written, test_subscription_id_unique_on_second_activated, test_uuid_pk_version, test_subscription_id_unique_violation.
    </human-check>
  </verify>
  <done>
    make test (Docker-free) is fully green — no stubs, no skips, all real assertions. make test-integration is fully green. VALIDATION.md per-requirement test map: every row has a green automated command. PROV-04 canonical proof test passes. Credential delivery tested: exactly once per lifecycle, no resend on retry. Snapshot written at configuring with version=1 (SNAP-01). WARNING 4 proof: test_write_enforcement_snapshot_calls_repository passes. WARNING 5 proof: test_instance_spec_round_trip and test_spec_rebuilt_from_payload pass. make check passes for all test files.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test assertions → production code | Tests must verify the security properties (no secret in log, credential-once guard) — not just happy-path correctness |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-12 | Information Disclosure | Tests that log credential fields during failure debugging | mitigate | Test assertions for credential-delivery tests ONLY assert call_count and call_args for recipient_email/instance_url — never assert or print admin_password in test output. Spy fixture uses AsyncMock which records args but does not print them. |
| T-3-13 | Denial of Service | Integration tests that leave tables dirty causing cross-test contamination | mitigate | pg_session fixture truncates all tables after each test (CASCADE). Confirmed by existing Phase-2 integration tests passing isolation. |
| T-3-SC | Tampering | npm/pip/cargo installs | accept | No new packages this plan. |
</threat_model>

<verification>
- make test (no Docker) is fully green — run `.venv/bin/pytest -m "not integration" -v 2>&1 | tail -30`
- make test-integration (requires Docker) is fully green
- make check passes — ruff check + ruff format --check for the full repo
- VALIDATION.md per-requirement test map: all rows now have a green status
- pytest tests/ -x --tb=short -q | grep "passed" — no failures, no errors, no skips (Wave 0 stubs fully replaced)
- grep -rn "pytest.skip" tests/provisioning/test_tasks.py tests/provisioning/test_service.py → returns empty (stubs replaced)
</verification>

<success_criteria>
- All 9 rows in VALIDATION.md per-requirement test map are verified green
- PROV-04 canonical proof: test_create_fails_then_retries passes with FakeDeploymentAdapter(fail_on={"create"}, fail_count=1) + FakeClock
- PROV-08 credential-once proof: test_credentials_sent_once and test_no_credential_resend_on_retry both pass
- SNAP-01 proof: test_enforcement_snapshot_written passes in integration suite
- PROV-01 UNIQUE proof: test_subscription_id_unique_violation passes in integration suite
- WARNING 4 proof: test_write_enforcement_snapshot_calls_repository passes (service delegates to repository, no raw ORM in service)
- WARNING 5 proof: test_instance_spec_round_trip and test_spec_rebuilt_from_payload pass (tuple preserved, Mapping reconstructed)
- Wave 0 stubs fully replaced: grep for pytest.skip in test_tasks.py and test_service.py returns empty
- make check passes — zero ruff errors, format clean
</success_criteria>

<output>
Create `.planning/phases/03-registry-create-path-fake-adapter/03-04-SUMMARY.md` when done.
</output>
